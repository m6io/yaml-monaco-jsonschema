export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "yaml-monaco-jsonschema",
  description: "YAML Monaco Editor with JSON Schema Validation",
  mainNav: [],
  links: {},
}
